# ===================== Secrets =====================
.env
# Never commit your real API key — only .env.example (the template) is tracked.

# ===================== Python =====================
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# ===================== ChromaDB =====================
# Vector DB files are regenerated by indexing the PDFs — no need to version
# them in git. On Render, this folder lives on a persistent disk instead.
chroma_db/

# ===================== Auto-generated metadata =====================
# metadata.json is regenerated automatically every time PDFs are indexed.
data/metadata.json

# ===================== OS / Editor junk =====================
.DS_Store
Thumbs.db
.vscode/
.idea/

# ===================== Pytest cache =====================
.pytest_cache/